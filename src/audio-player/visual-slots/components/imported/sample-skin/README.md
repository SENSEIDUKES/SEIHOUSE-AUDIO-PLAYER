# Sample Skin

Imported from Sea-Workshop-Light format #2 (structured React).

| Property | Value |
|----------|-------|
| **ID** | `sample-skin` |
| **Slot** | `seiCanvas` |
| **Format** | #2 (structured React) |

## Review Checklist

- [ ] Open the adapter (`SampleSkin.tsx`) and map `settings`/`playback` to the raw component's props in the `TODO(skin)` block.
- [ ] Add meaningful default settings in `sampleSkinDefaultSettings`.
- [ ] Build out the `SettingsPanel` stub with controls for each setting.
- [ ] Verify the scoped CSS (`sample-skin.css`) doesn't contain un-scoped selectors (check the header comment).
- [ ] Run `npm run build` to confirm everything compiles.

